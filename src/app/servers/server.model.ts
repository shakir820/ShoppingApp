export class Server{

  constructor(id:number, name:string, status:string){
    this.id = id;
    this.name = name;
    this.status = status;
  }
  public id:number
  public name:string;
  public status:string;
}
